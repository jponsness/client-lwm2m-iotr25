name: jbp-iotr25
base: core20
version: V1
summary: IOTR 2.5 LwM2M Client
description: |
  LwM2M Client for IOTR 2.5
confinement: strict
grade: stable

apps:
  test:
    command: test
    plugs:
      - network
      - network-bind
      - snapd-control
      - shutdown
parts:
  client:
    override-build: |
      ./prepare.sh
    build-packages:
      - cmake
      - autoconf
    plugin: go
    source: .
    go-importpath: launchpad.net/ce-web/alpaca
    
    
